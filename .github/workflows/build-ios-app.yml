name: build-ios-app
on:
  push:
    branches-ignore:
      - main
      - develop
jobs:
  build:
    runs-on: macOS
    steps:
      - uses: actions/checkout@v2
      - run: |
          gem install bundler:2.5.18
          bundle update
          bundle exec fastlane build
